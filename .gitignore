# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# uv lockfile — the demo dependency group uses sibling-checkout path sources
# (coding-agent-playbook, pydocs-mcp), so the lock is machine-specific here.
uv.lock

# Continue MCP blocks generated by `needle-demo init` — they embed absolute
# paths into this machine's .venv; regenerate per machine.
.continue/mcpServers/

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/
.nox/
.mypy_cache/
.ruff_cache/

# Indexes & model artifacts produced at runtime
*.pkl
index.pkl
indexes/
*.safetensors

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store

# Jupyter
.ipynb_checkpoints/
.coding-agent-playbook.local.toml
